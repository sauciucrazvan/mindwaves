name: mindwaves
description: Fully open-source mood tracker with AI integration and real-time notifications to keep track of your life.
version: 1.0.0

publish_to: 'none'


environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Time formatting
  intl: ^0.18.1

  # Graphs
  graphic: ^2.2.0

  # Animations
  lottie: ^2.6.0

  # Encryption with AES
  encrypt: ^5.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^2.0.1
  build_runner: ^2.4.6

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/